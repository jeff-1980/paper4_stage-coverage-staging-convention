% Appendix: ORIGINAL fixed-fraction results on the OFFICIAL held-out test set
% (one point per unit), reused as-is from
%   results/forensics_2026-09-11/item1_standard_cp_multiseed.csv -- NOT rerun.
% Generated by analyze_relabeled.py::appendix_official_test.
\begin{table}[t]
\caption{For reference: Split CP stage-conditional ECR on the official C-MAPSS held-out test set (one truncation point per unit), the fixed-fraction convention as originally evaluated. \textbf{The early stage bucket is empty for every subset and every seed}: the official test protocol truncates every unit close to end-of-life, and combined with the standard 125-cycle RUL clip, no unit's normalized lifetime position $t_{\mathrm{norm}}$ can fall below the 0.33 early/middle threshold under the shared per-subset lifetime reference used here. This is a structural property of the official benchmark protocol, not an artifact of our pipeline, and is why the main same-sample analysis (Table~\ref{tab:main-relabeled}) instead uses full held-out trajectories with a per-unit lifetime reference, which populates all three stages.}
\label{tab:official-test}
\centering
\begin{tabular}{lcc}
\toprule
Dataset & Stage & ECR (mean $\pm$ std over 5 seeds) \\
\midrule
FD001 & Middle & 0.875 $\pm$ 0.026 \\
FD001 & Late & 0.907 $\pm$ 0.027 \\
FD002 & Middle & 0.923 $\pm$ 0.048 \\
FD002 & Late & 0.872 $\pm$ 0.185 \\
FD003 & Middle & 0.787 $\pm$ 0.047 \\
FD003 & Late & 0.915 $\pm$ 0.021 \\
FD004 & Middle & 0.842 $\pm$ 0.041 \\
FD004 & Late & 0.877 $\pm$ 0.020 \\
\bottomrule
\end{tabular}
\end{table}
